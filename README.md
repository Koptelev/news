# Content Multi-Format Generator

Open-source сервис для автоматической генерации контента в различных форматах с использованием AI.

## Возможности

- 📱 **Telegram пост** - короткий, яркий пост с эмодзи (макс 300 символов)
- 📧 **Email сообщение** - деловое письмо с темой и телом (~150 слов)
- 📄 **Официальное письмо** - структурированное официальное письмо (~300 слов)
- 📬 **Массовая рассылка** - дружелюбный, но профессиональный текст (~250 слов)
- 🔌 **Расширяемость** - легко добавлять новые форматы через конфигурацию

## Технологии

- **Backend**: FastAPI
- **AI**: OpenRouter.ai (унифицированный интерфейс для 300+ моделей), OpenAI, Ollama
- **Конфигурация**: YAML для промптов
- **Интерфейсы**: REST API, CLI, Web форма

## Установка

### Требования

- Python 3.9+
- OpenRouter API ключ (или OpenAI API ключ, или настроенный Ollama)

### Шаги установки

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd serv
```

2. Создайте виртуальное окружение:
```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows
```

3. Установите зависимости:
```bash
pip install -r requirements.txt
```

4. Настройте переменные окружения:
```bash
cp .env.example .env
# Отредактируйте .env и добавьте ваш API ключ
```

## Конфигурация

### Переменные окружения

Создайте файл `.env`:

```env
# OpenRouter API (рекомендуется - доступ к 300+ моделям)
AI_PROVIDER=openrouter
OPENROUTER_API_KEY=your-api-key-here
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1
OPENROUTER_MODEL=x-ai/grok-4.1-fast:free

# Или используйте OpenAI API напрямую
# AI_PROVIDER=openai
# OPENAI_API_KEY=your-api-key-here
# OPENAI_BASE_URL=https://api.openai.com/v1
# OPENAI_MODEL=gpt-4o

# Или используйте Ollama (локально)
# AI_PROVIDER=ollama
# OLLAMA_BASE_URL=http://localhost:11434
# OLLAMA_MODEL=llama3.2

# Настройки приложения
LOG_LEVEL=INFO
OUTPUT_DIR=./outputs
```

## OpenRouter.ai

Проект настроен для использования [OpenRouter.ai](https://openrouter.ai/) - унифицированного интерфейса для доступа к 300+ моделям от различных провайдеров (OpenAI, Anthropic, Google, Meta и др.).

**Преимущества OpenRouter:**
- Доступ к множеству моделей через один API
- Лучшие цены и доступность
- Автоматический fallback при недоступности провайдера
- Полная совместимость с OpenAI API

**Популярные модели на OpenRouter:**
- `openai/gpt-4o` - GPT-4o от OpenAI
- `anthropic/claude-3.5-sonnet` - Claude от Anthropic
- `google/gemini-pro` - Gemini от Google
- `meta-llama/llama-3.1-70b-instruct` - Llama от Meta
- И многие другие...

Полный список моделей: https://openrouter.ai/models

## Использование

### CLI

```bash
# Генерация всех форматов
python -m src.cli --input "Ваша новость или анонс здесь"

# Генерация конкретного формата
python -m src.cli --input "Текст" --format telegram

# Сохранение в файл
python -m src.cli --input "Текст" --output result.json
```

### REST API

Запустите сервер:
```bash
uvicorn src.main:app --reload
```

Генерация контента:
```bash
curl -X POST "http://localhost:8000/generate" \
  -H "Content-Type: application/json" \
  -d '{"input_text": "Ваша новость здесь", "formats": ["telegram", "email"]}'
```

### Web интерфейс

Откройте в браузере: `http://localhost:8000/`

## Структура проекта

```
serv/
├── src/
│   ├── __init__.py
│   ├── main.py              # FastAPI приложение
│   ├── cli.py               # CLI интерфейс
│   ├── ai/
│   │   ├── __init__.py
│   │   ├── provider.py      # Абстракция AI провайдера
│   │   └── openai_client.py # OpenAI клиент
│   ├── generators/
│   │   ├── __init__.py
│   │   ├── base.py          # Базовый генератор
│   │   └── formats.py       # Генераторы форматов
│   ├── config/
│   │   ├── __init__.py
│   │   └── prompts.yaml     # Промпты для форматов
│   └── utils/
│       ├── __init__.py
│       ├── file_handler.py  # Работа с файлами
│       └── logger.py        # Логирование
├── outputs/                 # Сгенерированные файлы
├── templates/               # HTML шаблоны
├── requirements.txt
├── .env.example
└── README.md
```

## Примеры

### Пример входного текста

```
Компания TechCorp объявляет о запуске нового продукта - AI-ассистента для бизнеса. 
Продукт поможет автоматизировать рутинные задачи и повысить эффективность работы команд.
Запуск запланирован на 1 марта 2024 года.
```

### Пример выхода (Telegram)

```
🚀 TechCorp запускает AI-ассистента для бизнеса!

✨ Автоматизация рутинных задач
📈 Повышение эффективности команд
📅 Старт: 1 марта 2024

#TechCorp #AI #Бизнес
```

## Расширение функциональности

### Добавление нового формата

1. Добавьте промпт в `src/config/prompts.yaml`:
```yaml
new_format:
  system: "Вы - эксперт по созданию контента..."
  user: "Создайте {format_name} на основе следующего текста:\n\n{input_text}"
```

2. Создайте генератор в `src/generators/formats.py`:
```python
class NewFormatGenerator(BaseGenerator):
    format_name = "new_format"
```

3. Зарегистрируйте в `src/generators/__init__.py`

## Разработка

### Запуск тестов
```bash
pytest tests/
```

### Форматирование кода
```bash
black src/
isort src/
```

## Лицензия

MIT License

## Вклад в проект

Приветствуются Pull Requests! Пожалуйста, убедитесь что:
- Код соответствует стилю проекта
- Добавлены тесты для новой функциональности
- Обновлена документация

## Поддержка

Создайте Issue в GitHub для вопросов и предложений.

